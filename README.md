# Setup

```sh
git clone https://github.com/ljosberinn/personal-php-boilerplate && cd personal-php-boilerplate && composer install && npm install

npx babel assets/js --out-file assets/js/bundle.js -w
```
